#Bind N to the input, of type integer.
N = int(input())

#Uses the Bitwise & operator to test if N is odd.
#Checks if is N is odd or between 6 and 20.
if N & 1 or 6 <= N <= 20:
    print('Weird')
#Checks if N is greater than 20 or between 2 and 5.
elif N > 20 or 2 <= N <= 5:
    print('Not Weird')
