#Conditional that checks if the value of __name__ is __main__.
if __name__ == '__main__':
#Bind a to the first integer input.    
    a = int(input())
#Bind b to the second integer input.     
    b = int(input())

#Print the sum of a and b.
    print(a + b)
#Print the absolute value of the difference between a and b.
    print(abs(a - b))
#Print the product of a and b.
    print(a * b)
